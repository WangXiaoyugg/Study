/*作为web服务器软件，Node.js提供了net模块用于tcp通信，dgram模块用于udp通信net.createServer()用于创建tcp监听服务器，net.connect()用于客户端连接tcp服务器dgram模块通过dgram.createSocket()创建udp socket,通过bind()监听特定端口，通过send()向特定socket发送信息*/var dgram = require('dgram');var server = dgram.createSocket('udp4');server.on('error',(err)=>{   console.log('server error:\n' + err.stack);});//接受客户端的消息server.on('message',(msg,rinfo)=>{   console.log('server got: '+ msg.toString() + ' from '+    rinfo.address + ':' + rinfo.port);});//监听服务server.on('listening',()=>{   var address = server.address();   console.log('server listening on '+ address.address + ':'+ address.port);});server.bind(41234);// server listening 0.0.0.0:41234