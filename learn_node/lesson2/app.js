//npm 的基本使用/** 引入外部包的使用* 异常的处理* */var express = require('express');var utility = require('utility');var app = express();app.get('/',(req,res)=>{    var q = req.query.q;    if(q == null){        return res.send('请传入查询参数！')    }    var md5Value = utility.md5(q);    res.send(md5Value);});app.listen(3000,(req,res)=>{    console.log('app is running at port 3000')});